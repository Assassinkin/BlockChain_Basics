const DIFFICULTY = 3;
// How many leading 0s the hash need to have to be considered valid
const MINE_RATE = 3000;
//How often blocks are mined.

const INITIAL_BALANCE = 500;
//Inial balance for each wallet

module.exports = { DIFFICULTY, MINE_RATE, INITIAL_BALANCE };
